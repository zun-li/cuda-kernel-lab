{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CUDA Debug",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/build/matmul0",
            "cwd": "${workspaceFolder}",
            "debuggerPath": "/usr/local/cuda-13.0/bin/cuda-gdb"
        }
    ]
}